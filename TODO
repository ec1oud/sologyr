scale precip. acc. to a learned maximum
pref. for n days of forecast
figure out why forecast temperatures are only 2 days' worth
come up with a way to store and recall widget size (as reported to onAppWidgetOptionsChanged)
de-duplicate preference defaults
fix crash on 4.3 firmware if possible

done
----
when watch app is reentered, have app resend values without requerying web services
render precipitation from current hour ahead into the next day
fix rendering of temperature freezing->above transitions
replace Example image for widget
don't refresh radar too often
weather icons on the main screen
time (rendered red line) is initially wrong, until the minute changes?
scale widget temperature graph acc. to n-day max, not total max

